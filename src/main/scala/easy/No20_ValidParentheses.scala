package easy

object No20_ValidParentheses {
  def isValid(s: String): Boolean = {
//    val map = Map(')' -> '(', ']' -> '[', '}' -> '{')
//    val stack=scala.collection.mutable.Stack[Char]()
//    val keys=map.keys.toArray
//    for(c<-s){
//      if(!keys.contains(c)){
//        stack.push(c)
//      }
//
//    }
    true
  }
}
